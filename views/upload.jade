h1 Upload

form(action='/upload', method='post', enctype='multipart/form-data')
    p Upload a zip file containing the CSV data:
    input(type='file', name='csvFile')
    input(type='submit', name='upload', value='Upload')
